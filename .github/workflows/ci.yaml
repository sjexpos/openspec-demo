name: CI
on:
  push:
    branches: [ "main" ]

jobs:
  ci:
    name: ''
    uses: ./.github/workflows/wf-ci.yaml
    secrets: inherit
    with:
      SONAR_PROJECT_KEY: openspec-demo
      SONAR_PROJECT_NAME: Openspec Demo
      DOCKER_IMAGES: '["sjexpossdd/openspec-demo"]'
      DEFAULT_BRANCH: main
