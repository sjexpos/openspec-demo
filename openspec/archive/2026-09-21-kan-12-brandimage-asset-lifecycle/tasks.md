## 0. Setup: Create Feature Branch (MANDATORY - FIRST STEP)

- [x] 0.1 Create feature branch `feat/kan-12-brandimage-asset-lifecycle` from main and verify current branch status — Refs: proposal.md, design.md Migration Plan — Verification: branch listed as current via branch status command — Files touched: none (VCS only).

## 1. Shared Lifecycle Enum AssetStatus (TDD Red to Green)

- [x] 1.1 RED — Write failing `AssetStatusTests` covering exact-three-constants guard in order PENDING, UPLOADED, DELETED, literal-name persistence contract, full 3x4 transition matrix including null target, self-transition rejection, `isTerminal` and `isVisible` — Refs: specs/asset-lifecycle Requirement Shared asset lifecycle states, Requirement Legal asset lifecycle transitions; design.md D2, D7 — TDD red: new test class fails because `AssetStatus` does not exist; verify failure message shows missing type, not a typo — Acceptance: tests enumerate constant-count guard, literal names, parameterized legal/illegal transitions, null/self rejection, terminal/visible assertions — Files touched: `test/.../domain/models/AssetStatusTests.java` (create).
- [x] 1.2 GREEN — Implement `AssetStatus` enum with exhaustive switch, no framework imports, minimal code to pass 1.1 — Refs: specs/asset-lifecycle Requirement Shared asset lifecycle states, Requirement Legal asset lifecycle transitions; design.md D1, D2 — TDD green: run `AssetStatusTests`, all pass, no other tests broken, output pristine — Acceptance: constants in required order, `canTransitionTo` allows only PENDING to UPLOADED/DELETED and UPLOADED to DELETED, null and self return false, `isTerminal` true only for DELETED, `isVisible` true only for UPLOADED, coverage target met for the new class — Files touched: `domain/models/AssetStatus.java` (create).

## 2. BlobType Per-Type Key Ownership Check (Behaviour-Preserving DRY Refactor)

- [x] 2.1 RED — Extend `BlobTypeTests` with failing `isKeyOf` cases: own-prefix recognition parameterized over all six blob types, cross-type rejection, null/blank/traversal rejection — Refs: specs/blob-storage Requirement Blob type to key prefix mapping, Scenario Key ownership is distinguishable per blob type; design.md D4, D7 — TDD red: new `isKeyOf` assertions fail because method does not exist; verify existing `isCanonicalKey` tests still pass untouched at this point — Acceptance: new tests fail for missing method only; no existing test modified — Files touched: `test/.../domain/models/BlobTypeTests.java` (modify, add only).
- [x] 2.2 GREEN — Extract per-type `isKeyOf` derived from existing prefix constants and delegate global `isCanonicalKey` to it with null safety — Refs: specs/blob-storage Requirement Blob type to key prefix mapping; design.md D4 — TDD green: full `BlobTypeTests` suite passes including pre-existing `isCanonicalKey` tests unchanged — Acceptance: behaviour-preserving refactor proven by untouched existing tests passing, new ownership tests passing, single source of truth for prefixes preserved — Files touched: `domain/models/BlobType.java` (modify).

## 3. Flyway Migration V0.1.1 Plus Existing Suite Proof

- [x] 3.1 Create Flyway migration `V0.1.1__brand_images_asset_lifecycle.sql` performing rename of `image_url` to `image_key`, adding `status varchar(16)` with CHECK over the three state names and dropping the backfill default, adding BaseEntity audit columns, unique index on `image_key`, composite index on `(brand_id, status)`, partial index on `created_at WHERE status = PENDING` — Refs: specs/asset-lifecycle Requirement Lifecycle persistence format, specs/brands-management Requirement Brand image persistence and lifecycle; design.md D6, Migration Plan — TDD/verification: migration file picked up from `flyway/release_0.1/` by both Maven plugin and test classpath with no config change — Acceptance: SQL applies rename plus add-column path, CHECK enumerates exactly the three state names, no silent status default remains, three indexes present — Files touched: `flyway/release_0.1/V0.1.1__brand_images_asset_lifecycle.sql` (create).
- [x] 3.2 Prove migration applies by running the existing repository test suite against real Postgres with Flyway so a broken migration fails the suite — Refs: design.md D7, Migration Plan step 2 — Verification: existing `@DataJpaTest` suite green on top of the new migration; record command output summary — Acceptance: no existing test broken by rename or new columns, migration proven on the fresh-create path — Files touched: none (verification only).

## 4. BrandImage Entity With Guarded Transitions (TDD Red to Green)

- [x] 4.1 RED — Write failing `BrandImageTests` covering PENDING-only creation on canonical BRAND_IMAGE keys, rejection of foreign-type keys, null/blank/non-canonical/traversal keys, null/transient brand rejection, `markUploaded`/`markDeleted` legal paths, double-confirm and post-DELETED `IllegalStateException`, reflection guard asserting no public `setStatus`/`setImageKey` — Refs: specs/brands-management Requirement Brand image persistence and lifecycle and all five scenarios; specs/asset-lifecycle Requirement Legal asset lifecycle transitions; design.md D1, D3, D7 — TDD red: new test class fails because `BrandImage` does not exist; verify failure is missing type — Acceptance: tests encode factory-only creation, key-ownership validation, guarded transitions, and setter-absence guard — Files touched: `test/.../domain/models/brand/BrandImageTests.java` (create).
- [x] 4.2 GREEN — Implement `BrandImage` entity mapped to `brand_images` extending `BaseEntity` with LAZY ManyToOne Brand, immutable unique `imageKey`, STRING-mapped status with no public setters, `pending` factory validating persisted brand and `BRAND_IMAGE.isKeyOf`, and `markUploaded`/`markDeleted` delegating to `canTransitionTo` — Refs: specs/brands-management Requirement Brand image persistence and lifecycle; design.md D1, D3 — TDD green: run `BrandImageTests` plus phases 1-2 suites, all pass — Acceptance: entity protects its own invariants, no `@SQLDelete`/`@SQLRestriction` present, key immutable, status changes only via guarded methods, no presigned URL field — Files touched: `domain/models/brand/BrandImage.java` (create).

## 5. BrandImageRepository With Database-Level Proofs (TDD Red to Green)

- [x] 5.1 RED — Write failing `BrandImageRepositoryTests` against real Postgres plus Flyway covering persist-and-reload PENDING with audit timestamps, native-query string proof, CHECK proof with an illegal status value, unique violation, FK violation, null-status violation, `findByBrandIdAndStatus` filtering with one row per status, `findByImageKey` hit/miss, `existsByImageKey`, transition round-trip with modified timestamp update, DELETED tombstone-keeps-row proof — Refs: specs/brands-management Requirement Brand image persistence and lifecycle Scenarios Brand image recorded before upload, Duplicated blob key rejected, Retired brand image keeps its row; specs/asset-lifecycle Scenarios State stored as text, Unknown state rejected by the database; design.md D5, D7 — TDD red: new test class fails because `BrandImageRepository` does not exist; verify failure is missing repository type — Acceptance: tests assert DB-level contracts via native queries and integrity violations, not only JPA assertions — Files touched: `test/.../integration/repositories/BrandImageRepositoryTests.java` (create).
- [x] 5.2 GREEN — Implement `BrandImageRepository` extending `JpaRepository` with `findByBrandIdAndStatus`, `findByImageKey`, `existsByImageKey` and no custom query or delete methods — Refs: specs/brands-management Requirement Brand image persistence and lifecycle; design.md D5 — TDD green: run `BrandImageRepositoryTests`, all pass including native string, CHECK, unique, FK, NOT NULL, filtering, and tombstone proofs — Acceptance: three derived queries resolve correctly, removal represented solely by DELETED with no physical-delete path added — Files touched: `domain/repositories/BrandImageRepository.java` (create).

## 6. Mapping Versus Schema Parity and Existing-Test Review (MANDATORY)

- [x] 6.1 Run Hibernate mapping versus schema parity check with `ddl-auto: validate` over the migrated schema to confirm zero drift between entity mappings and V0.1.1 — Refs: design.md D6, D7, Migration Plan step 2; enriched source section 6.5 — Verification: validate run passes with zero drift on both fresh and V0.1.0-upgraded paths — Acceptance: column names, nullability, lengths, and enum STRING mapping match the migration exactly — Files touched: none (verification only; test property override if needed).
- [x] 6.2 Review and update existing unit tests (MANDATORY) — confirm pre-existing `BlobTypeTests` `isCanonicalKey` cases pass untouched and no other existing suite regressed after phases 1-5 — Refs: design.md D4, D7 — Verification: full relevant unit suites green; any necessary test maintenance documented — Acceptance: behaviour-preserving `BlobType` refactor proven, zero unintended regressions — Files touched: existing test files only if required (otherwise none).

## 7. Documentation and Spec Finalization

- [x] 7.1 Update `docs/data-model.md` Brand Images section plus ER diagram with `image_key`, `status`, and audit columns, documenting that `deleted_at`/`deleted_by` ship unused because status is the delete marker — Refs: proposal.md What Changes docs item; design.md Migration Plan step 4 — Verification: section and diagram render consistently with V0.1.1 columns — Acceptance: stale `image_url` documentation removed, key-not-URL semantics and tombstone model stated — Files touched: `docs/data-model.md` (modify).
- [x] 7.2 Update `docs/backend-standards.md` to register the enum-persistence convention of Enumerated STRING plus varchar plus CHECK, never ordinals and never native PG enums — Refs: proposal.md What Changes docs item; design.md D6, Migration Plan step 4 — Verification: convention text present and cross-referenced from the change — Acceptance: future asset tables inherit the same persistence rule — Files touched: `docs/backend-standards.md` (modify).
- [x] 7.3 Confirm change specs reflect implementation: `specs/asset-lifecycle/spec.md`, `specs/brands-management/spec.md`, `specs/blob-storage/spec.md` complete and consistent with the shipped behavior — Refs: all three delta specs; enriched source section 10 — Verification: spec scenarios trace to test phases 1, 2, 4, 5 — Acceptance: no spec drift; lifecycle reuse, key ownership, and tombstone rules match code and migration — Files touched: `openspec/changes/kan-12-brandimage-asset-lifecycle/specs/*` only if gaps found (otherwise none).

## 8. Backend: Run Unit Tests and Verify Database State (MANDATORY - AGENT MUST EXECUTE)

- [x] 8.1 Capture pre-test database baseline for impacted entities (`brand_images` row counts and Flyway version state) — Verification: baseline recorded — Files touched: none (observation only).
- [x] 8.2 Run targeted unit tests for changed modules (`AssetStatusTests`, `BlobTypeTests`, `BrandImageTests`, `BrandImageRepositoryTests`) and confirm failures resolved with no new regressions in targeted scope — Verification: command output summary captured with passed/failed/skipped counts — Files touched: none (execution only).
- [x] 8.3 Run the required broader unit test suite and verify post-test database state with restoration if any mutation remains — Verification: totals, runtime, and flaky behavior recorded; post-test indicators match baseline or restoration documented — Files touched: none (execution only).
- [x] 8.4 Create report `openspec/changes/kan-12-brandimage-asset-lifecycle/reports/YYYY-MM-DD-step-8-unit-test-and-db-verification.md` with executed commands, summarized results, database pre/post comparison, and cleanup actions; mark this phase complete only after tests pass and the report exists — Verification: report file exists at the stated path pattern — Acceptance: traceable test and DB-state evidence archived in the change folder — Files touched: `openspec/changes/kan-12-brandimage-asset-lifecycle/reports/YYYY-MM-DD-step-8-unit-test-and-db-verification.md` (create).

## 9. Backend: Manual Endpoint Testing With curl (MANDATORY - AGENT MUST EXECUTE, NO-ENDPOINT SCOPE)

- [x] 9.1 Confirm this slice ships no HTTP endpoint, service, DTO, or exception-handler change per proposal and design non-goals, so there is no endpoint to exercise with curl; verify instead that no new routes are exposed and record that outcome in the manual-verification report — Verification: route inventory shows no brand-image endpoint added — Acceptance: explicitly documented no-endpoint scope with DB-level proofs from phase 5 standing in for endpoint checks — Files touched: `openspec/changes/kan-12-brandimage-asset-lifecycle/reports/YYYY-MM-DD-step-9-manual-curl-verification.md` (create).

## 10. Update Technical Documentation (MANDATORY)

- [x] 10.1 Final documentation consistency pass over `docs/data-model.md`, `docs/backend-standards.md`, and change specs confirming English-only artifacts, DDD layering notes, and Flyway plus validate evidence are referenced — Verification: docs diff reviewed and consistent — Acceptance: documentation matches shipped schema, entity, and lifecycle behavior with no stale references — Files touched: docs/spec files only if inconsistencies found (otherwise none).
